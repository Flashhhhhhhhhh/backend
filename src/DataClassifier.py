#!/usr/bin/python

class DataClassifier:
    def classify_data(self, csvFile):
        # implement

    def update_model(self, json):
        # Read tags and update model
        #
        # Tags:
        #  combine(h1, h2, h_new)
        #  create(hnew)
        #  move(h_old, h_new)
        #  remove(h)
        #  sensitive(h)

    def visualize_date(self json):
        # Input data into newly structured header model

